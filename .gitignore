# build outputs (c-shared plugin binary + generated header)
dist/
*.so
*.h

# Go
*.test
*.out

# bare `go build` binaries (no extension, one per plugin dir)
workbuddy/workbuddy
workbuddy/workbuddy-plus
workbuddy-global/workbuddy-global
qoderwork/qoderwork
qwenwork/qwenwork
